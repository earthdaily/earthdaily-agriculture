# ============================================================================
# earthdaily-agriculture — runtime requirements
# ============================================================================
# GENERATED by the release pipeline from pyproject.toml. Do not edit by hand:
# pyproject.toml is the single source of truth, and this file is rewritten on
# every release.
#
# This is the non-editable alternative to `pip install earthdaily-agriculture`.
# Optional extras are NOT included here — install them explicitly:
#
#   pip install -e ".[jupyter]"
#   pip install -e ".[test]"
#   pip install -e ".[s3]"
#   pip install -e ".[azure]"
#   pip install -e ".[cloud]"
#   pip install -e ".[cube]"
#   pip install -e ".[report]"
# ============================================================================

requests>=2.31.0
urllib3>=1.26.0
pandas>=2.2.3
numpy>=2.2.5,<2.5
pyarrow
duckdb>=1.4.2
geopandas>=1.0.1
shapely>=2.1.0
rasterio
contextily
pygeohash>=3.0
matplotlib>=3.10.1
folium
plotly>=6.5.0
tqdm>=4.64.0
loguru>=0.6.0
pyyaml>=6.0
python-dotenv>=1.0.0
python-dateutil>=2.8.0
Pillow>=9.3.0
scipy
reportlab
boto3>=1.42.2,<1.43
earthdaily
tabulate>=0.9.0
openpyxl>=3.1.5
